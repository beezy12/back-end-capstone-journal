
nav.navbar.navbar-default.mainNav
    .container-fluid
    .navbar-header
        button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1', aria-expanded='false')
            //- span.sr-only Toggle navigation
            //- span.icon-bar
            //- span.icon-bar
            //- span.icon-bar

        a.navbar-brand.viewList(ui-sref="main").i.material-icons view_module
        a.navbar-brand.btn(ng-click="oneNote.saveEntry()") save
        //- a.navbar-brand.btn(ng-click="editing = !editing") edit
        a.navbar-brand.btn(ng-click='oneNote.deleteEntry()') delete



.container-fluid
    .row
        .col-md-11.col-md-offset-1
            label#journalTitle(ng-click="editing = true" ng-hide="editing"){{oneNote.noteInfo.data.title}}
            input#journalTitle(ng-change="oneNote.edited = true" ng-blur="editing = false" ng-show="editing" ng-model="oneNote.noteInfo.data.title")


    .row
        .col-md-11.col-md-offset-1
            label#journalBox(ng-click="editing = true" ng-hide="editing"){{oneNote.noteInfo.data.entry}}
            textarea#journalBox(ng-change="oneNote.edited = true" ng-blur="editing = false" ng-show="editing" ng-model="oneNote.noteInfo.data.entry")



